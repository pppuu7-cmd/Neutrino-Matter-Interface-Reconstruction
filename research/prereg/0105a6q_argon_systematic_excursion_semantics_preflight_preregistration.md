# 0105a6q — Ar Analysis-A systematic-excursion semantics preflight

Status: **PREREGISTERED / AUTHORITY-SEMANTICS PREFLIGHT / NONDISCOVERY**

Parent numerical gate: `PASS_0105A6O3_TIERB_ARGON_CERTIFIED_CENTRAL_NULL_REPRODUCTION_NONDISCOVERY`.

## Purpose

Before any 10,000-pseudo-experiment systematic excursion is generated, determine whether the provenance-qualified Analysis-A method plus the exact official excursion files uniquely specify enough of the pseudo-data and excursion treatment to write the separate Stage-8 Monte-Carlo preregistration without inventing hidden analyst choices.

This gate performs no pseudo-data generation and no likelihood fit. It does not inspect any observed BSM/model-agnostic residual.

## Frozen authority inputs

1. Exact 0105a6p artifact `10160794369`, ZIP SHA256 `a66b3408b22b14575f156c0be7c389589dd9865ada1f4769fe8366a1df65ec82`, `manifest.json` SHA256 `5b1df47a50dd4dbb2a7fbd5ee64346c2854bdbad0540bfa6f16637eb10b6bada`.
2. Exact bounded 0105a6m evidence artifact `10160137826`, ZIP SHA256 `df11b8726e52b3b1662509d3130bfab39c45bd298e5d21995a0613a375fa7d6b`, `evidence.json` SHA256 `672b8e5ec68e01395250e97fbafcbda956f8ffc51b52773d6379588b1fb87364`.
3. Exact Indiana University thesis identity established at 0105a6j: 34,641,327 bytes, SHA256 `6dd2fde86601dea28d323fc38e289935723ad1bf0f8c5f2dcfd845fe2e6badf9`.
4. A narrowly scoped supplemental machine-text extraction may access only physical PDF pages **154–158 inclusive**, because these pages are already inside the mechanically frozen 0105a6k1 interval 129–158 and contain the systematic-method/result discussion identified prospectively by 0105a6l/m. No page outside 154–158 may be accessed by this gate.

The web PDF visual renderer was attempted on the institutional bitstream and failed to expose it as `application/pdf`; therefore any supplemental extraction in this gate is explicitly machine-text evidence, not visual verification. No visual-layout-dependent conclusion may be made.

## Frozen exact-file structural questions

For each of the eight exact systematic files, parse the same 960 coordinate tuples as its corresponding central component and record:

- exact raw total;
- raw-total ratio to the corresponding central component;
- whether the total differs from the central total by more than `0.05` event;
- L1 shape distance after unit normalization;
- filename-defined component family only: prompt-BRN or CEvNS.

No filename is allowed to establish a scientific sign/pairing rule beyond literal `+1sig` / `-1sig` tokens.

## Frozen semantic fields

### Q1 — ensemble size

COMPLETE only if scoped evidence explicitly states that **10,000 pseudo-data sets** are fit for each systematic excursion study.

### Q2 — generator/fit direction

COMPLETE only if scoped evidence explicitly states both:

- pseudo-data are generated using the excursion PDF(s); and
- those pseudo-data are fit with the central-value PDFs.

### Q3 — stochastic total-count generation law

COMPLETE only if scoped evidence explicitly resolves whether the pseudo-data component/event totals are:

- Poisson/extended-Poisson fluctuated;
- fixed to nominal event totals; or
- generated by another explicitly described probability law.

Candidate markers include `Poisson`, `extended`, `number of events`, `event count`, `fluctuat`, `fixed`, `generate`, `RooFit`, but a marker alone is not COMPLETE unless it determines the count law. The fact that the likelihood itself is extended does not by itself infer how pseudo-data were generated.

### Q4 — two-sided ±1σ source estimator

COMPLETE only if scoped evidence uniquely determines how the `+1σ` and `-1σ` mean-fit shifts are converted into one nonnegative source uncertainty, including whether signs are retained, absolute values are taken, or a half-difference/mean-absolute-shift rule is used.

The phrase “average difference ... for a ±1 pair” is recorded as evidence but is not automatically promoted to a unique algebraic rule unless the scoped text makes the operation unambiguous.

### Q5 — one-sided excursion estimator

COMPLETE only if scoped evidence explicitly determines how an excursion source represented by only one released alternative file is converted into a source uncertainty relative to the central study.

This specifically targets:

- `brnpdfBRNTimingWidthSyst.txt`;
- `cevnspdfCEvNSTimingMeanSyst.txt`.

No synthetic reflection or invented missing `-1σ` partner is allowed.

### Q6 — excursion normalization semantics

COMPLETE only if scoped authority determines whether the raw event normalization encoded in each excursion file is part of the pseudo-data excursion or whether every excursion must first be renormalized to the central component total.

A raw-total difference in the exact bytes is structural evidence that the choice matters; it is not by itself authority to select either treatment.

### Q7 — independence/combination rule

COMPLETE only if scoped evidence explicitly states that the excursion-derived systematic errors are treated as uncorrelated and identifies the relevant quadrature combination rule for the reported Analysis-A result.

### Q8 — published systematic benchmark

COMPLETE only if scoped evidence identifies the reported CEvNS result as `159 ± 43 (stat) ± 14 (syst)` events and/or an equivalent explicit 14-event total systematic benchmark. The final combined null significance `3.5` may be recorded as an additional benchmark but does not replace the 14-event requirement.

### Q9 — source inventory/mapping

COMPLETE only if the scoped evidence plus exact release filenames can uniquely map every released excursion file into a named systematic source and identify which files form two-sided pairs versus one-sided alternatives, without inferring missing partners.

## Machine-text output limits

The hosted result may contain:

- exact source/artifact hashes;
- physical page numbers;
- booleans/status for Q1–Q9;
- short normalized evidence windows capped at **700 characters per field per page**;
- exact structural metrics for the eight excursion files.

It must not upload the thesis PDF or full extracted pages.

## Frozen classification

If Q1–Q9 are all COMPLETE:

`PASS_0105A6Q_SYSTEMATIC_EXCURSION_SEMANTICS_PREFLIGHT_NONDISCOVERY`.

If one or more of Q3/Q4/Q5/Q6/Q9 remain semantically unresolved while source identity/extraction succeeds:

`BLOCKED_0105A6Q_SYSTEMATIC_EXCURSION_SEMANTICS_INCOMPLETE`.

Transport/hash/scope extraction failure:

`BLOCKED_0105A6Q_SOURCE_TRANSPORT_OR_EXTRACTION`.

## Consequence

A PASS allows a separate prospectively frozen 10,000-pseudo-experiment systematic-excursion reproduction gate.

A semantic BLOCKED result does **not** authorize guessing the missing law. The next route must be a new provenance-qualified source/implementation-authority acquisition gate targeting only the unresolved Q-fields, or an explicitly labeled independent Tier-B sensitivity bracket that cannot be confused with reproduction of the published 14-event systematic.

## Hard ceilings

- no pseudo-experiment execution in 0105a6q;
- no publication-systematic PASS from file names alone;
- no continuous shape morphing;
- Tier-A collaboration-internal likelihood remains BLOCKED;
- `OBSERVED_BSM_RESIDUAL_PERMISSION_PERCENT = 0`.
