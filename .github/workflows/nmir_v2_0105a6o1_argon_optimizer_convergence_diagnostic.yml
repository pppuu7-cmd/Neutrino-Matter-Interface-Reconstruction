name: NMIR v2 0105a6o1 Ar optimizer convergence diagnostic

on:
  push:
    branches:
      - research/0105-bsm-residual-reconstruction
    paths:
      - 'research/prereg/0105a6o1_argon_optimizer_convergence_diagnostic_preregistration.md'
      - 'scripts/run_0105a6o1_argon_optimizer_convergence_diagnostic_v2.py'
      - 'tests/test_0105a6o1_argon_optimizer_convergence_diagnostic.py'
      - '.github/workflows/nmir_v2_0105a6o1_argon_optimizer_convergence_diagnostic.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  optimizer-diagnostic:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install frozen numerical dependencies
        run: python -m pip install --disable-pip-version-check numpy scipy pytest
      - name: Run preregistered diagnostic guards
        run: python -m pytest -q tests/test_0105a6o1_argon_optimizer_convergence_diagnostic.py
      - name: Recover exact 0105a6p official-byte artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p artifacts/0105a6o1/input
          gh api -H 'Accept: application/vnd.github+json' \
            /repos/${GITHUB_REPOSITORY}/actions/artifacts/10160794369/zip \
            > artifacts/0105a6o1/input/a6p.zip
          echo 'a66b3408b22b14575f156c0be7c389589dd9865ada1f4769fe8366a1df65ec82  artifacts/0105a6o1/input/a6p.zip' | sha256sum -c -
          unzip -q artifacts/0105a6o1/input/a6p.zip -d artifacts/0105a6o1/input/a6p
          echo '5b1df47a50dd4dbb2a7fbd5ee64346c2854bdbad0540bfa6f16637eb10b6bada  artifacts/0105a6o1/input/a6p/manifest.json' | sha256sum -c -
      - name: Execute prospectively frozen optimizer diagnostic
        run: |
          set -o pipefail
          PYTHONPATH=. python scripts/run_0105a6o1_argon_optimizer_convergence_diagnostic_v2.py \
            --files-dir artifacts/0105a6o1/input/a6p/reconstructed_exact_official_bytes \
            --a6p-manifest artifacts/0105a6o1/input/a6p/manifest.json \
            --output artifacts/0105a6o1/result.json \
            --git-sha "$GITHUB_SHA" \
            | tee artifacts/0105a6o1/stdout.txt
          sha256sum artifacts/0105a6o1/result.json | tee artifacts/0105a6o1/result_sha256.txt
      - name: Remove exact input bytes before result upload
        run: rm -rf artifacts/0105a6o1/input
      - name: Upload optimizer diagnostic result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nmir-v2-0105a6o1-argon-optimizer-diagnostic
          path: |
            artifacts/0105a6o1/result.json
            artifacts/0105a6o1/result_sha256.txt
            artifacts/0105a6o1/stdout.txt
          if-no-files-found: warn
