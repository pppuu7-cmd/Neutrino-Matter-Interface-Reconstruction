name: NMIR 0087c-r1 Cerdeño mass-support-only authority

on:
  push:
    paths:
      - 'research/amendments/0087c_positive_exponent_parser_conformance.md'
      - 'scripts/audit_cerdeno_mass_support_only_0087c.py'
      - 'scripts/audit_cerdeno_mass_support_only_0087c_r1.py'
      - 'tests/test_audit_cerdeno_mass_support_only_0087c.py'
      - '.github/workflows/0087c-r1-cerdeno-mass-support-only.yml'
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install exact parser/runtime dependencies
        run: python -m pip install --upgrade pip && python -m pip install pymupdf pytest
      - name: Run parser-conformance regression tests
        run: pytest -q tests/test_audit_cerdeno_mass_support_only_0087c.py
      - name: Execute frozen 0087c scientific contract via r1 wrapper
        env:
          PYTHONPATH: scripts
        run: python scripts/audit_cerdeno_mass_support_only_0087c_r1.py
      - name: Upload exact 0087c-r1 artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nmir-0087c-r1-cerdeno-mass-support-only
          path: cerdeno_mass_support_only_0087c_r1.json
          if-no-files-found: error
